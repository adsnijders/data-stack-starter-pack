name: CI_pipeline

on:
  pull_request:
    branches:
    - main

jobs:
  CI_jobs:
    runs-on: self-hosted
    env:
      POSTGRES_USER: ${{ secrets.POSTGRES_USER }}
      POSTGRES_PASSWORD: ${{ secrets.POSTGRES_PASSWORD }}
      POSTGRES_DB: ${{ secrets.POSTRES_DB }}
      GIT_SHA: ${{ github.event.pull_request.head.sha }}
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Install uv
        uses: astral-sh/setup-uv@v5

      - name: Install Python
        run: uv python install

      - name: Install dependencies
        run: uv sync --group ci --no-dev

      - name: Linting
        uses: astral-sh/ruff-action@v3

      - name: dbt Build
        run: uv run dbt build --select "state:modified+" --target test --profiles-dir /Users/adsnijders/Documents/trainings/6_data_analytics/data-stack-starter-pack/dbt/webshop/

      - name: dbt Docs
        run: uv run dbt docs generate

      


